name: Format with Black

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
      
jobs:
  linter_name:
    name: runner / black
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: psf/black@stable
